{
  "name": "recipes_backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",  
    "dev": "nodemon index.js", 
    "build": "npm run build-frontend && echo 'No build step for backend'",  // Run frontend build
    "build-frontend": "cd frontend && npm install && npm run build",  // Frontend build command
    "postinstall": "npm install"  
  },
  "dependencies": {
    "axios": "^1.7.9",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.8.4",
    "nodemon": "^3.1.7"
  }
}
